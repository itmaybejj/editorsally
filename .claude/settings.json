{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json; d=json.load(sys.stdin); p=d.get('file_path',''); sys.exit(1 if 'assets/ed11y' in p else 0)\" <<< \"$CLAUDE_TOOL_INPUT\"",
            "blocking": true,
            "blockMessage": "assets/ed11y/ is auto-generated by scripts/get.sh — edit the source in the editoria11y GitHub repo instead."
          }
        ]
      }
    ]
  }
}
